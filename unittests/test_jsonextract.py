from app.classes import jsonextract

je = jsonextract()


def test_num_of_keys():
    total = 0
    for x in range[0 : len(someotherfunction)]:
        total += len(someotherfunction[x])
    assert total == 3105


def test_num_of_pages():
    assert len(someotherfunction) == 11


def test_key_types():
    for key in keylist:
        assert isinstance(key, DataFrame)


return_bucket_json_body(self, key):
get_bucket_json_key_list(self):
json_file_to_dataframe(self, keylist):
yield_pages